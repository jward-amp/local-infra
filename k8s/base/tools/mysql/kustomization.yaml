apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: mysql
metadata:
  name: tools-mysql

resources:
  - namespace.yaml
  - deployment.yaml
  - persistent-volume-claim.yaml
  - service.yaml